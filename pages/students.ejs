<h1>Students</h1>
<table></table>
    <tr>
        <th>Student ID</th>
        <th></th>
        <th>Salary</th>
    </tr>
    <% students.forEach(student => { %> <!-- Changed from 'student' to 'students' -->
    <tr>
      <td><%= student.sid %></td>
      <td><%= student.name %></td>
      <td><%= student.age %></td>
    </tr>
  <% }); %>
</table>