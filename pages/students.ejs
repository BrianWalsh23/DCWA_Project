<table></table>
    <tr>
        <th>ID</th>
        <th>Name</th>
        <th>Salary</th>
    </tr>
    <% empList.forEach((emp) =>{ %>
        <tr>
            <td><%= emp.id %></td>
            <td><%= emp.name %></td>
            <td><%= emp.salary %></td>
        </tr>
    <% }) %>
</table>